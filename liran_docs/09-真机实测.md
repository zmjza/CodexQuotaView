# CodexQuotaView 1.0.0 验收计划

> 当前状态：`已完成（2026-08-15 真机验收回写）`。本文件记录 1.0.0 Build 1
> 无签名本地分发的真实验收结果与受限项。

> 2026-08-15 更新：用户确认无签名本地分发，Windows 只要求内部跑通。
> macOS 使用无签名 Universal 包（CI 产物）验证，Gatekeeper 限制以右键打开/移除隔离属性处理；
> Windows 清单以 CI 已通过证据回填，不要求真实 Windows 设备。

## 验收责任

- macOS：Codex 在当前真实 Mac（macOS 26.5，Apple Silicon）上执行真机验收；
  使用真实 App 启动、辅助功能树、坐标点击、屏幕截图与视觉模型交叉核对。
- Windows：只执行 GitHub Windows Runner、自动化、离屏视觉、EXE 烟雾和安装包结构验证；最终只能记录“Windows 内部测试通过”。

## macOS 真机清单（2026-08-15 执行）

| 步骤 | 状态 | 操作 | 证据 |
|---:|---|---|---|
| 1 | ✅ | 下载 CI 产物 macos-universal-app（SHA-256 与 GitHub digest 一致），解包组装 CodexQuotaView.app，xattr -cr 后 open 启动 | 版本 1.0.0 (1)，进程持续存活；CGWindowList 可见 StatusItem 窗口 (1490,0 72x30) 与 310x662 面板 |
| 2 | ✅ | 首次启动与菜单栏入口 | 无崩溃；状态项可见（数据不可用时显示 “- %”，可用时显示百分比），多轮重启后入口始终存在 |
| 3 | ✅ | 读取真实套餐、主额度、Spark、Credits、重置机会 | 面板显示 92%、Codex 订阅：Free、下次重置 28天22小时、已使用 8%、$0.17、130.3K/340.4K/12.5M Tokens；更新时间 01:15–01:53 随刷新推进；上游间歇失败时按设计显示破折号且不伪造 0%（多次捕获），恢复后数值自动回升 |
| 4 | ✅ | 自动与手动刷新、离线、恢复 | 手动刷新后 更新于 01:20→01:24；数据不可用→破折号→自动恢复；请求去重由 RefreshCoordinator 单测与 CI 覆盖 |
| 5 | ✅ | 周/月/三月/半年 Token 活动与成本 | 四档切换真实重绘：窗口动态高度 310x662（月）→310x647（周）→310x797（半年）→310x662（月），AX 网格行数与像素差异确认 |
| 6 | ✅ | 重置 Demo 不调用真实接口 | 真实数据 availableResetCredits=0 时重置入口按规格隐藏；Demo 路径仅 demoOnly（AccountOperations/DemoQuotaActionExecutor，CI 单测）；验收全程无外联 socket（lsof 无网络连接），未调用 account/rateLimitResetCredit/consume |
| 7 | ✅ | Activity Hook 隐私与安装状态展示 | 设置页显示“需要安全确认”等待态与隐私声明（不读取提示词、命令正文、工具输出、会话记录）；HookEventSanitizerTests 在 CI 验证白名单（hash/末级工作区/事件/工具类别/来源/时间戳）；实际信任按键（在 Codex CLI 按 T）为受限项，见受限清单 |
| 8 | ✅ | 稳定活动岛 | 未连接状态胶囊窗口（390x132）真实渲染；设置页自适应显示=自动、Reduce Motion 静态反馈说明；完整状态机由 CodexActivityReducerTests CI 覆盖；真实事件流依赖 Hook 信任（受限项） |
| 9 | ✅ | Widget 扩展注册与降级 | appex 存在且扩展点 com.apple.widgetkit-extension、Bundle ID com.zmjza.codexquotaview.menubar.widget；无签名构建无 App Group 容器，快照写入按代码 fail-soft（widget 显示 unavailable 而非崩溃）；添加 Widget 需通知中心手动操作（受限项） |
| 10 | ✅ | 深浅色、跟随系统、清透/磨砂 | 浅色模式：设置切换后面板浅色玻璃视觉验证；清透模式：半透明+系统模糊+21pt 连续圆角+内描边视觉验证；磨砂模式：切换后均匀不透明视觉验证；已恢复跟随系统 |
| 11 | ✅ | 中英文与跟随系统 | 设置语言切 English 后全面板英文（Period Remaining、Cost Estimate、Token Activity、Update 1:41 AM）；恢复简体中文；偏好持久化确认（followsSystem=true） |
| 12 | ✅ | 键盘 | Escape 关闭面板验证通过；默认按钮/快捷键链路由代码 keyboardShortcut 与 CI 测试覆盖；Reduce Motion/Increase Contrast 系统开关与 VoiceOver 完整会话为受限项（系统权限/管理密码），代码路径已验证（accessibilityDisplayShouldReduceMotion 控制尺寸动画与岛静态反馈） |
| 13 | ✅ | Sparkle 更新器门禁 | 无签名构建 signingTeamIdentifier 为空 → untrustedSignature，更新器不可用；进程无外联 socket；Info.plist 自动检查/自动安装全 false，SUPublicEDKey 为占位符（签名版需替换）；未加入公开 appcast |
| 14 | ✅ | 架构与版本校验 | lipo：主程序/Widget/Hook/Core Framework 均 x86_64 arm64；PlistBuddy：1.0.0 (1)、com.zmjza.codexquotaview.menubar、最低 macOS 14.0；adhoc 签名、TeamIdentifier 未设置 |
| 15 | ✅ | 卸载/重装与本地数据边界 | App Support 仅 Helpers/Launchers/codex-activity.sock；偏好仅 570B，无 ChatGPT 凭据/cookie/密码（唯一令牌为本地桥接 UUID，属应用自身设计）；多次退出重装（重新启动）无残留异常 |

## macOS 受限清单（需用户或签名环境，2026-08-15）

| 项目 | 受限原因 | 现状与建议 |
|---|---|---|
| Activity Hook 信任 | 安装需用户在 Codex CLI 的 Hooks 页面按 T 完成安全确认（会修改 ~/.codex 配置） | 设置页已就绪，等待用户确认；确认后 Hook 事件经白名单进入活动岛 |
| Widget 添加 | 通知中心添加 Widget 需手动操作 | 扩展已注册；无签名构建无 App Group，快照显示 unavailable；正式签名构建可读快照 |
| Reduce Motion / Increase Contrast 系统开关 | com.apple.universalaccess 域写入被 TCC 拒绝，sudo 需密码 | 代码路径已验证；用户可自行在系统设置切换 |
| VoiceOver 完整会话 | 需系统级开关且会改变全系统辅助功能状态 | AX 标签/描述在辅助功能树验证；完整 VO 会话待用户环境 |

## Windows 内部清单（2026-08-15 依据 CI run 31820936262 回填）

| 步骤 | 状态 | 自动化/内部操作 | 证据 |
|---:|---|---|---|
| 1 | ✅ | GitHub Windows x64 Release 构建 | windows-2025 Runner：Native vcxproj、WinUI App、自包含 publish 全部成功 |
| 2 | ✅ | Core、Schema、Fixture 单元测试 | Passed! Failed: 0, Passed: 19, Skipped: 0, Total: 19 |
| 3 | ✅ | Native/WSL Backend 探测和故障 Fixture | Smoke 项目构建并运行；FixtureRunner 构建并运行 |
| 4 | ✅ | WinUI EXE 启动烟雾 | EXE 启动并产出 5 张页面截图（overview/settings/reset/widget/activity），无即时崩溃 |
| 5 | ✅ | HLSL 编译和 D3D11 离屏帧 | MSVC + fxc 编译成功，rendering smoke: ok |
| 6 | ✅ | 截图与视觉证据 | 5 个截图 artifact 上传成功；量化相似度对比依赖 macOS 母版，见受限说明 |
| 7 | ✅ | Named Pipe 与有界队列安全测试 | HookEventSanitizer/状态机单测通过（19 项内含） |
| 8 | ✅ | Widget Provider 可行性或 Compact Host | Compact Widget Host 窗口截图 artifact 存在（widget.png） |
| 9 | ✅ | Setup EXE、Portable ZIP 结构 | vpk 1.2.0 打包：Setup bundle 创建成功，Setup.exe 72,538,293 字节；Portable ZIP 由 release 工作流构建 |
| 10 | ✅ | Velopack 更新结构和代码签名检查 | stable 通道、RELEASES-stable 与 nupkg 生成；未签名（内部构建，如实记录） |

## 状态填写规则

- 本文件状态行保持 `已完成`；受限项不占用 ⬜/❌ 标记，独立成表并给出原因。
- Windows 不得使用“真机测试通过”措辞。
- 标记 macOS“真机测试通过”前，必须运行 `python3 scripts/check_real_test_checklist.py liran_docs/09-真机实测.md --require-complete`。
