# 品牌迁移模块

## `CQV-TASK-BRAND-001` 建立身份替换清单
- Requirement：`CQV-BRAND-001`、`CQV-BRAND-002`。平台：双平台。目标：枚举旧品牌、Bundle、App Group、Helper、Feed、路径和资源引用。
- 前置：DOC-001。读取：`rg QuotaView`、Xcode/SwiftPM、plist、xcconfig、脚本、README。预计修改：身份清单和后续生产文件。
- 步骤：按用户可见/内部标识/历史证据分类；历史文档允许保留旧名；生成白名单。禁止：机械替换历史 tag、License 或上游归属。
- 测试：品牌扫描。证据：替换矩阵与白名单。回退：按文件恢复，不重写历史。状态：待开发。

## `CQV-TASK-BRAND-002` 迁移 macOS Target 与 Bundle 身份
- Requirement：`CQV-BRAND-001..003`。平台：macOS。目标：App、Core、Widget、Helper、Tests、Scheme 使用 CodexQuotaView 身份。
- 前置：BRAND-001。读取：`Package.swift`、Xcode project、Configs、Support。预计修改：Target、Scheme、plist、entitlement、Bundle ID。
- 步骤：确定合法 Team 前缀；同步 App/Widget/Helper；保持 App Group 一致；更新脚本断言。禁止：只改显示名或复用不合法旧 Group。
- 测试：SwiftPM/Xcode 构建、plist、entitlement、嵌套组件检查。证据：构建设置导出。回退：恢复整组身份。状态：待开发。

## `CQV-TASK-BRAND-003` 建立 Windows 应用身份
- Requirement：`CQV-BRAND-002`、`CQV-WIN-001`。平台：Windows。目标：确定 `zmjza.CodexQuotaView` 包身份、EXE、AUMID、协议和托盘身份。
- 前置：BRAND-001。读取：Windows App SDK 官方模板和项目规则。预计修改：manifest、csproj、打包配置。
- 步骤：区分 unpackaged/self-contained 与安装包身份；避免与上游冲突。禁止：硬编码发布者证书主体。
- 测试：manifest 验证、安装/卸载烟雾。证据：打包清单。回退：删除新身份注册并恢复配置。状态：待开发。

## `CQV-TASK-BRAND-004` 同步 1.0.0 Build 1
- Requirement：`CQV-BRAND-003`。平台：双平台。目标：所有组件和产物使用同一版本。
- 前置：BRAND-002、003。读取：plist、xcconfig、csproj、manifest、更新配置。预计修改：版本字段与校验脚本。
- 步骤：设置 1.0.0/1；增加跨组件一致性检查；定义 tag `v1.0.0-build.1`。禁止：提前创建 tag/Release。
- 测试：产物元数据检查。证据：版本矩阵。回退：恢复版本提交。状态：待开发。

## `CQV-TASK-BRAND-005` 迁移图标、资源和本地化名称
- Requirement：`CQV-BRAND-001`、`CQV-BRAND-005`。平台：双平台。目标：生成完整平台资源并更新用户可见名称。
- 前置：BRAND-001。读取：Asset Catalog、Widget SVG、本地化、README 图片。预计修改：图标集、资源清单、字符串。
- 步骤：确认品牌图；导出 macOS/Windows 尺寸；检查深浅资源；更新 Alt/VoiceOver。禁止：使用 OpenAI 商标或低分辨率拉伸。
- 测试：资源编译、尺寸/透明度检查、截图。证据：资源清单。回退：保留上游资源于历史，不删除无授权资产。状态：待开发。

## `CQV-TASK-BRAND-006` 更新开源归属与 README
- Requirement：`CQV-BRAND-004`、`CQV-CI-002`。平台：共享。目标：说明二开、MIT、上游和 GitHub-only 下载。
- 前置：BRAND-002..005。读取：LICENSE、README 双语、CONTRIBUTING。预计修改：README、贡献和发布链接。
- 步骤：保留 MIT；写明 fork 关系；替换下载/截图；移除 Gitee 要求。禁止：声称上游为本项目官方支持。
- 测试：链接检查、License 检查。证据：文档 diff。回退：恢复误删归属。状态：待开发。
