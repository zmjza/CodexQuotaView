# 更新系统模块

## `CQV-TASK-UPD-001` 迁移 Sparkle 产品身份
- Requirement：`CQV-UPD-001`、`CQV-BRAND-002`。平台：macOS。目标：新 Bundle、Feed、Key account 和更新 Team 不引用上游。
- 前置：BRAND-002、004。读取：Info.plist、AppUpdateController、脚本。预计修改：Sparkle 配置。
- 步骤：确定新 Feed/EdDSA；更新校验；不复用无授权私钥。禁止：把密钥写入仓库。
- 测试：Fixture appcast 和签名验证。证据：脱敏身份矩阵。回退：关闭检查更新并明确不可用。状态：待开发。

## `CQV-TASK-UPD-002` Sparkle N→N+1 测试基线
- Requirement：`CQV-UPD-001`。平台：macOS。目标：验证新产品安装版本可升级到更高 Build。
- 前置：UPD-001、签名包。读取：上游更新规格。预计修改：测试 Feed/发布记录。
- 步骤：本地签名 Fixture；旧包启动；检查、下载、验证、替换、重启。禁止：未授权上传公开 appcast。
- 测试：真实 Mac N→N+1。证据：版本和签名。回退：恢复旧 App。状态：待开发。

## `CQV-TASK-UPD-003` 建立 Velopack 基础
- Requirement：`CQV-UPD-002`。平台：Windows。目标：Setup 安装和更新使用 Velopack。
- 前置：WIN-006、BRAND-004。读取：Velopack 官方 API。预计修改：App 启动、打包配置。
- 步骤：启动初始化；安装事件；版本通道；GitHub 源。禁止：Portable 假装安装版自动更新。
- 测试：包生成、安装事件、离线启动。证据：日志。回退：保留手动下载。状态：待开发。

## `CQV-TASK-UPD-004` Windows N→N+1 内部测试
- Requirement：`CQV-UPD-002`。平台：Windows。目标：在 Runner/隔离环境验证更新结构。
- 前置：UPD-003、两个测试版本。读取：发布配置。预计修改：集成测试。
- 步骤：安装 N；发布 N+1 Fixture；检查下载/替换/重启；验证用户设置。禁止：连接未发布生产通道。
- 测试：内部更新烟雾。证据：版本日志。回退：卸载测试包。状态：待开发。

## `CQV-TASK-UPD-005` 更新 UI 状态对称
- Requirement：`CQV-UPD-001..002`、`CQV-UX-002`。平台：双平台。目标：可用、检查中、最新、发现更新、失败状态明确。
- 前置：UPD-001、003、设置 UI。读取：AppUpdateController。预计修改：更新 ViewModel/UI。
- 步骤：平台能力探测；按钮禁用；脱敏错误；本地化。禁止：未接入时伪造成功。
- 测试：状态 Fixture。证据：UI 快照。回退：显示不可用说明。状态：待开发。

## `CQV-TASK-UPD-006` 自动更新准入门禁
- Requirement：`CQV-UPD-001..002`、`CQV-REL-003`。平台：双平台。目标：公开更新通道与 GitHub Release 分离授权。
- 前置：UPD-002、004。读取：AGENTS opt-in 规则。预计修改：发布文档/工作流输入。
- 步骤：绑定精确版本、Build、tag、资产；未获准不部署 Feed。禁止：从“发布”推断 appcast 准入。
- 测试：缺少准入输入应阻止 Feed 部署。证据：工作流负面测试。回退：保持公开 Feed 不变。状态：待开发。
