# 额度数据模块

## `CQV-TASK-DATA-001` 套餐与方案归一
- Requirement：`CQV-DATA-001`。平台：双平台。目标：统一 Free、Go、Plus、Pro 5x/20x、Business、Enterprise、Edu 和未知值。
- 前置：PROTO-001..003。读取：`WidgetSnapshot.swift`、Provider Adapter。预计修改：双端映射和 Fixture。
- 步骤：复用生产枚举语义；覆盖旧 team/usage-based；未知显示破折号。禁止：原始枚举全大写或硬编码 PLUS。
- 测试：每种方案表驱动测试。证据：跨端结果。回退：未知值降级。状态：待开发。

## `CQV-TASK-DATA-002` 主周期额度投影
- Requirement：`CQV-DATA-002`、`CQV-DATA-009`。平台：双平台。目标：正确投影 used/remaining/reset/duration/risk。
- 前置：DATA-001、PROTO-003。读取：Provider Adapter、Presentation。预计修改：双端 Projector。
- 步骤：验证范围；不假定周期固定；区分未知和 0；风险按剩余比例。禁止：连接失败显示 0%。
- 测试：边界、溢出、缺失 Fixture。证据：快照差异。回退：显示不可用。状态：待开发。

## `CQV-TASK-DATA-003` Spark 独立窗口
- Requirement：`CQV-DATA-003`。平台：双平台。目标：识别 Spark 窗口并独立展示。
- 前置：DATA-002。读取：`CodexDomainCatalog.sparkRateWindowID`。预计修改：Adapter、UI ViewModel。
- 步骤：按稳定 ID 选取；复用百分比/倒计时；缺失时稳定隐藏。禁止：将 Spark 合并进主周期。
- 测试：存在、缺失、异常周期。证据：Fixture。回退：隐藏 Spark 区。状态：待开发。

## `CQV-TASK-DATA-004` Credits 与无限状态
- Requirement：`CQV-DATA-004`。平台：双平台。目标：正确表达余额、无限、无 Credits 和未知。
- 前置：PROTO-001。读取：CreditsSnapshot、Adapter。预计修改：Decimal 解析和格式化。
- 步骤：无损解析；本地化格式；无限状态不显示 0。禁止：浮点精度丢失。
- 测试：大数、小数、null、unlimited。证据：格式化快照。回退：原始安全字符串或破折号。状态：待开发。

## `CQV-TASK-DATA-005` 重置机会与 Demo 门禁
- Requirement：`CQV-DATA-004`、`CQV-DATA-010`。平台：双平台。目标：按真实可用次数控制入口，操作仅 Demo。
- 前置：DATA-002。读取：AccountOperations、重置 UI。预计修改：ViewModel、Demo Executor、测试。
- 步骤：仅有效快照且次数 >0 显示；确认后只更新 Demo 状态；入口消失自动返回。禁止：调用 consume。
- 测试：静态端点扫描、0/1/多次/失效状态。证据：负面测试。回退：完全禁用入口。状态：待开发。

## `CQV-TASK-DATA-006` 刷新协调与错误降级
- Requirement：`CQV-DATA-008`、`CQV-DATA-009`。平台：双平台。目标：消费者共享刷新、保留旧快照并明确最新错误。
- 前置：MAC-003、WIN-004。读取：RefreshCoordinator、StatusStore。预计修改：双端 coordinator。
- 步骤：合并并发请求；超时取消；usage 局部失败；更新时间一致。禁止：请求风暴或静默吞错。
- 测试：并发、取消、超时、局部失败。证据：状态序列。回退：串行刷新。状态：待开发。

## `CQV-TASK-DATA-007` 数据状态 UI 契约
- Requirement：`CQV-DATA-009`、`CQV-UX-005`。平台：双平台。目标：available/refreshing/offline/error 的布局、Tooltip 和辅助功能一致。
- 前置：DATA-001..006、VIS-001。读取：AGENTS 状态圆点规则。预计修改：ViewModel 和 UI。
- 步骤：连接圆点只表示最新获取；占位保持尺寸；提供语义标签。禁止：只靠颜色或状态胶囊。
- 测试：状态截图、VoiceOver/Narrator 属性。证据：矩阵。回退：稳定不可用布局。状态：待开发。
