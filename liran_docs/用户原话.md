# 用户原话与确认记录

## 首次需求原话

> 我想二开。名称为 CodexQuotaView。然后我要 mac 和 win 同时做。mac 可以真机测试，win 内部跑腿即可。最终编译成给我 macapp 程序和 win exe 程序。要求是本地打开就能用的。视觉方案直接照搬先，一模一样的照抄。
>
> 首版 mac 和 win 就需要完整复刻。
>
> 我都是本地应用。只要别人下载就能打开用就好了。gitee 我不用。只用 github。

## 已确认结果

- 产品名称：`CodexQuotaView`。
- 首版身份：`1.0.0 Build 1`。
- 平台：macOS 与 Windows 首版同时完整交付。
- 视觉：macOS 当前生产界面为唯一母版；静态相似度不低于 95%，动态玻璃与流体球主要状态不低于 90%，主要几何差异不超过 2 px。
- 运行形态：纯本地桌面应用，不依赖自建服务器；下载安装包后即可安装、启动和使用。
- 验收责任：macOS 由 Codex 在真实 Mac 上测试；Windows 只记录内部自动化、构建、烟雾与 GitHub CI 结果。
- 托管与发布：只使用 GitHub；不使用 Gitee。
- 用户已选择 `$liran-plan Codex 真机测试`，后续不得再次询问由谁负责验收。

## 事实与推测边界

- 上述内容是用户确认的产品需求。
- 现有 QuotaView `0.3.5 Build 5` 的实现与发布事实来自生产代码、`HANDOFF.md` 和 `VERSION_HISTORY.md`。
- Windows 原生实现、Windows Widget API 可用性、签名证书和最终安装体验尚未执行验证，均不得提前记录为完成。
